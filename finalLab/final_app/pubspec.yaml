name: final_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_bloc: ^7.0.0
  equatable: ^2.0.3
  shared_preferences: ^2.0.6
  url_launcher: ^6.0.3
  http:

flutter:
  uses-material-design: true
